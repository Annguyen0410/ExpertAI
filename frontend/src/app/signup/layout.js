export const metadata = {
  title: "Create account · ExpertAI",
  description: "Start with ExpertAI's free tier for professional AI guidance.",
};

export default function SignUpLayout({ children }) {
  return children;
}
